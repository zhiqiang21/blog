/**
 * push 添加一个新元素到栈顶
 * pop 移除栈顶元素，同时返回被移除的元素
 * peek 返回栈顶元素，不对栈做任何的修改
 * isEmpty 如果栈里面没有任何元素就返回true 否则返回false
 * clear 移除栈里面的所有元素
 * size 返回栈里面的元素的个数
 */

class Stack {
  constructor() {
    this.item = [];
  }

  push() {}
  pop() {}
}
